# Mizan — environment (§15)
# The app renders useful content with ZERO keys configured (fixture mode).
# Every key below is optional in dev; missing keys degrade gracefully.
#
# NON-NEGOTIABLE (§2, §17): there is NO AI provider key in V1.
# Do not add ANTHROPIC_API_KEY, OPENAI_API_KEY, or any hosted-model key.

# --- Market data providers (free tier; any subset may be set) ---
# The ProviderRegistry skips any provider whose key is absent, so pages always
# render from cache/fixtures regardless of which of these are configured.
FMP_API_KEY=                # primary equity quotes, profiles, search (free ~250/day)
POLYGON_API_KEY=            # EOD quotes + aggregate candles for chart ranges (free ~5/min)
EODHD_API_KEY=              # EOD candle backfill + real-time fallback
FINNHUB_API_KEY=            # optional — equity quotes/profiles/news
TWELVEDATA_API_KEY=         # optional — secondary quotes, FX, multi-asset
MARKETSTACK_API_KEY=        # optional — low free tier; not wired by default
FRED_API_KEY=               # free — US macro series
ALPHAVANTAGE_API_KEY=       # last-resort quote/daily-candle/profile/search fallback (free ~25/day)

# --- Storage (Vercel) ---
POSTGRES_URL=               # Vercel Postgres (Neon) + pgvector
KV_REST_API_URL=            # Vercel KV
KV_REST_API_TOKEN=

# --- Jobs / scheduling ---
# Shared secret. Also set as a GitHub Actions secret. Every /api/cron/* route
# verifies `Authorization: Bearer ${CRON_SECRET}` and returns 401 otherwise.
CRON_SECRET=

# --- Required by SEC EDGAR: a descriptive User-Agent ---
SEC_USER_AGENT=             # e.g. "Mizan contact@mizan.com"

# --- Story cover images (free tier) ---
# Used ONLY by scripts/fetch-cover-images.ts (and future jobs) to resolve
# landscape cover photos, cached in KV; never called on page render (§17).
# Missing key → the UI draws deterministic on-brand SVG covers.
PEXELS_API_KEY=

# --- AI summaries (Groq; owner-approved, free tier) ---
# Used ONLY by scripts/generate-briefs.ts (and the cluster job) to write original
# story summaries, cached; never called on page render (§17). Missing key → the
# UI falls back to the dek + source list. Rankings/clustering stay model-free.
# Do NOT add any OTHER hosted-model key (no OPENAI_API_KEY / ANTHROPIC_API_KEY).
GROQ_API_KEY=
GROQ_MODEL=                 # optional, default llama-3.3-70b-versatile

# --- Public ---
NEXT_PUBLIC_SITE_URL=http://localhost:3000
