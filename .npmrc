# eslint-config-next resolves its plugins from the project root; pnpm's strict
# layout hides them. Hoist eslint plugins so `next lint` / ESLint can find them.
public-hoist-pattern[]=*eslint-plugin*
public-hoist-pattern[]=*eslint-config*
public-hoist-pattern[]=@next/eslint-plugin-next
