import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export const metadata = { title: "About" };

export default function AboutPage() {
  return (
    <PagePlaceholder title="About" phase="Phase 6 · Written in full">
      Mizan surfaces which stories matter, which instruments they move, and how markets
      responded — signal over an undifferentiated firehose.
    </PagePlaceholder>
  );
}
